# Port report — `p256-kotlin`

> Generated by `scaffold/generate_repo_reports.py` from `workspace_dep_graph.json` ⨯ `port_priority.json`. Do not hand-edit. Re-run after the workspace dep graph is rebuilt.

## Overview

- **1** member crate(s): `p256`
- **Codex-kotlin distance**: **hop 1** from codex-kotlin via cross-repo Rust deps
- **Inbound**: 20 `use` sites from 3 other repos
- **Outbound**: 2 `use` sites to 1 other repos
- **Unresolved external**: 7 published Cargo crates with no `*-kotlin/` port

## Member crates

| package | module | inbound `use` sites | outbound external repos | Jira | rank | status |
|---|---|---:|---:|---|---:|---|
| `p256` | `p256` | 20 | 1 | COD-94 | 160 | To Do |

## Inbound — what to port first

Every symbol this repo's crates need to expose, ranked by total workspace demand. **Codex-kotlin sites are listed first per symbol** because codex is the priority consumer driving the port queue. Anything not listed here is not currently `use`'d by any kotlinmania repo and can be deferred.

### Inbound caller summary (20 edges, 3 caller crates)

| caller repo | caller crate | use sites | codex consumer? |
|---|---|---:|---|
| `codex-kotlin` | `codex-device-key` | 8 | ✅ |
| `aws-sigv4-kotlin` | `aws-sigv4` | 7 |  |
| `aws-config-kotlin` | `aws-config` | 5 |  |

### Symbol-level demand (13 distinct symbols)

Sorted by codex demand, then total demand. Symbols with codex-kotlin sites are the priority queue. Each symbol entry below shows up to 12 call sites (codex first).

| symbol | total | codex | other | distinct callers |
|---|---:|---:|---:|---:|
| `p256::ecdsa::SigningKey` | 4 | 1 | 3 | 3 |
| `p256::ecdsa::Signature` | 3 | 1 | 2 | 3 |
| `p256::ecdsa::signature::Signer` | 2 | 1 | 1 | 2 |
| `p256::ecdsa::signature::Verifier` | 2 | 1 | 1 | 2 |
| `p256::ecdsa::VerifyingKey` | 1 | 1 | 0 | 1 |
| `p256::elliptic_curve::rand_core::OsRng` | 1 | 1 | 0 | 1 |
| `p256::pkcs8::DecodePublicKey` | 1 | 1 | 0 | 1 |
| `p256::pkcs8::EncodePublicKey` | 1 | 1 | 0 | 1 |
| `p256::SecretKey` | 1 | 0 | 1 | 1 |
| `p256::ecdsa::DerSignature` | 1 | 0 | 1 | 1 |
| `p256::ecdsa::signature::RandomizedSigner` | 1 | 0 | 1 | 1 |
| `p256::ecdsa::signature::Signature` | 1 | 0 | 1 | 1 |
| `p256::elliptic_curve::sec1::ToEncodedPoint` | 1 | 0 | 1 | 1 |

#### Per-symbol detail (top 13 symbols)

##### `p256::ecdsa::SigningKey` (4 sites, 1 codex, 3 other, 3 caller crates)

Codex call sites:

- `codex-kotlin/codex-device-key` `src/lib.rs:714` — `use p256::ecdsa::SigningKey`

Other workspace call sites:

- `aws-config-kotlin/aws-config` `src/login/dpop.rs:19` — `use p256::ecdsa::SigningKey`
- `aws-sigv4-kotlin/aws-sigv4` `src/http_request/test.rs:311` — `use p256::ecdsa::SigningKey`
- `aws-sigv4-kotlin/aws-sigv4` `src/sign/v4a.rs:7` — `use p256::ecdsa::SigningKey`

##### `p256::ecdsa::Signature` (3 sites, 1 codex, 2 other, 3 caller crates)

Codex call sites:

- `codex-kotlin/codex-device-key` `src/lib.rs:713` — `use p256::ecdsa::Signature`

Other workspace call sites:

- `aws-config-kotlin/aws-config` `src/login/dpop.rs:19` — `use p256::ecdsa::Signature`
- `aws-sigv4-kotlin/aws-sigv4` `src/sign/v4a.rs:7` — `use p256::ecdsa::Signature`

##### `p256::ecdsa::signature::Signer` (2 sites, 1 codex, 1 other, 2 caller crates)

Codex call sites:

- `codex-kotlin/codex-device-key` `src/lib.rs:716` — `use p256::ecdsa::signature::Signer`

Other workspace call sites:

- `aws-sigv4-kotlin/aws-sigv4` `src/sign/v4a.rs:6` — `use p256::ecdsa::signature::Signer`

##### `p256::ecdsa::signature::Verifier` (2 sites, 1 codex, 1 other, 2 caller crates)

Codex call sites:

- `codex-kotlin/codex-device-key` `src/lib.rs:717` — `use p256::ecdsa::signature::Verifier`

Other workspace call sites:

- `aws-sigv4-kotlin/aws-sigv4` `src/http_request/test.rs:310` — `use p256::ecdsa::signature::Verifier`

##### `p256::ecdsa::VerifyingKey` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-device-key` `src/lib.rs:715` — `use p256::ecdsa::VerifyingKey`

##### `p256::elliptic_curve::rand_core::OsRng` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-device-key` `src/lib.rs:718` — `use p256::elliptic_curve::rand_core::OsRng`

##### `p256::pkcs8::DecodePublicKey` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-device-key` `src/lib.rs:719` — `use p256::pkcs8::DecodePublicKey`

##### `p256::pkcs8::EncodePublicKey` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-device-key` `src/lib.rs:4` — `use p256::pkcs8::EncodePublicKey`

##### `p256::SecretKey` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `aws-config-kotlin/aws-config` `src/login/dpop.rs:21` — `use p256::SecretKey`

##### `p256::ecdsa::DerSignature` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `aws-sigv4-kotlin/aws-sigv4` `src/http_request/test.rs:311` — `use p256::ecdsa::DerSignature`

##### `p256::ecdsa::signature::RandomizedSigner` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `aws-config-kotlin/aws-config` `src/login/dpop.rs:18` — `use p256::ecdsa::signature::RandomizedSigner`

##### `p256::ecdsa::signature::Signature` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `aws-sigv4-kotlin/aws-sigv4` `src/http_request/test.rs:310` — `use p256::ecdsa::signature::Signature`

##### `p256::elliptic_curve::sec1::ToEncodedPoint` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `aws-config-kotlin/aws-config` `src/login/dpop.rs:20` — `use p256::elliptic_curve::sec1::ToEncodedPoint`

## Outbound — what this repo depends on

Other kotlinmania repos this repo's Rust source `use`s. Symbol-level detail per target lives in **that target repo's PORT_REPORT.md inbound section** (no duplication). Sorted by Jira rank, then by `use`-site count.

| target repo | use sites | distinct symbols | target packages | Jira | rank | status |
|---|---:|---:|---|---|---:|---|
| `sha2-kotlin` | 2 | 2 | `sha2` | COD-135 | 23 | To Do |

## Unresolved external dependencies

Published Cargo crates this repo's Rust source `use`s but for which no `<crate>-kotlin/` repo exists. Each is either a missing port to file in Jira, or an intentional external dep (system library / FFI / build-only). If a port lives at a non-conventional repo name, add it to `PACKAGE_REPO_OVERRIDES` in `scaffold/build_workspace_dep_graph.py`.

| package | edges | distinct symbols | importing members in this repo |
|---|---:|---:|---|
| `criterion` | 385 | 11 | `p256` |
| `elliptic-curve` | 86 | 54 | `p256` |
| `proptest` | 21 | 6 | `p256` |
| `hex-literal` | 16 | 1 | `p256` |
| `serdect` | 12 | 4 | `p256` |
| `primeorder` | 12 | 8 | `p256` |
| `ecdsa` | 6 | 5 | `p256` |
