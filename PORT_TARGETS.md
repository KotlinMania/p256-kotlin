# Port targets — `p256-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `p256` that codex-rs uses, and therefore the minimum subset that `p256-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-94] (rank `160`, tier `direct`, status `To Do`)

**Cargo package name**: `p256`
**Rust module name (in `use` statements)**: `p256`

## Summary

- **8** distinct symbols imported by codex-rs
- **8** total `use` statements
- **1** codex source files affected
- **1** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `p256::pkcs8::EncodePublicKey`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-device-key`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `device-key/src/lib.rs:4` (`codex-device-key`) — `use p256::pkcs8::EncodePublicKey`

### `p256::ecdsa::Signature`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-device-key`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `device-key/src/lib.rs:713` (`codex-device-key`) — `use p256::ecdsa::Signature`

### `p256::ecdsa::SigningKey`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-device-key`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `device-key/src/lib.rs:714` (`codex-device-key`) — `use p256::ecdsa::SigningKey`

### `p256::ecdsa::VerifyingKey`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-device-key`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `device-key/src/lib.rs:715` (`codex-device-key`) — `use p256::ecdsa::VerifyingKey`

### `p256::ecdsa::signature::Signer`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-device-key`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `device-key/src/lib.rs:716` (`codex-device-key`) — `use p256::ecdsa::signature::Signer`

### `p256::ecdsa::signature::Verifier`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-device-key`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `device-key/src/lib.rs:717` (`codex-device-key`) — `use p256::ecdsa::signature::Verifier`

### `p256::elliptic_curve::rand_core::OsRng`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-device-key`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `device-key/src/lib.rs:718` (`codex-device-key`) — `use p256::elliptic_curve::rand_core::OsRng`

### `p256::pkcs8::DecodePublicKey`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-device-key`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `device-key/src/lib.rs:719` (`codex-device-key`) — `use p256::pkcs8::DecodePublicKey`
